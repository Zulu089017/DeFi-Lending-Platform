{
  "name": "@stellar-payment-gateway/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'docs site: no build needed'",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@stellar-payment-gateway/tsconfig": "workspace:*",
    "typescript": "^5.5.3",
    "vitest": "^2.1.9"
  }
}
