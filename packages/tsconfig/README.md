# @stellar-payment-gateway/tsconfig

Base `tsconfig.json` for all StellarPay TypeScript packages.

## Usage

```json
{
  "extends": "@stellar-payment-gateway/tsconfig/base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```
