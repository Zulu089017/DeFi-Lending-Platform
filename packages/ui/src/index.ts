// Shared UI primitives for StellarPay apps.
// Re-export from apps/web/components/ui for now; in production,
// components like Button, Card, Badge, Input, Tabs, and Toaster
// would be hoisted here for cross-app reuse.

export {};
